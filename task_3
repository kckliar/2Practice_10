#include <iostream>
int main() {
    int lon = 10;

    int a[lon];
    for(int i = 0; i < lon; i++)
    {
        printf(i, &a[i], (a[i] = rand() % 0xff));
    }
        
    int * pa = a;

    for(int i = 0; i < someSize; i++)
        printf(*(pa + i));

    return 0;

    return 0;
}
